env = Environment(CCFLAGS="-std=c++11 -g")

env.Library("ssgtw", Glob("lib/src/*.cpp"))

env.Program("sistragatiwe.cpp",
    LIBS=("sfml-graphics", "sfml-window", "sfml-system",
		"PocoFoundation", "ssgtw", "emon"),
    LIBPATH="."
)
